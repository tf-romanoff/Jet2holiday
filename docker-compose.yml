version: "3.8"

services:
  app:
    build: ./app
    ports:
      - "3000:3000"
    volumes:
      - ./flag.txt:/app/flag.txt:ro
    restart: unless-stopped
    networks:
      - app_net

networks:
  app_net:
    driver: bridge